name: Keep Render Instance Alive

on:
  # schedule:
  #   - cron: '*/5 * * * *'  # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -X GET https://lab-booking-site.onrender.com/health > /dev/null 2>&1
